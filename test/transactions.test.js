/**
* Test for all paytr controllers api
*/

const chai = require("chai");
const should = chai.should();
const app = require('../app');
const chaiHTTP = require("chai-http");

chai.use(chaiHTTP);


describe("Tests for all paytr api", () => {
    // Test goes here
});
